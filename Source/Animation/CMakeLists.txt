file(GLOB ANIMATION *.cpp)
target_sources(Chronos PRIVATE ${ANIMATION})
target_include_directories(Chronos PUBLIC .)
if(CHRONOS_BUILD_EDITOR)
    target_sources(ChronosEditor PRIVATE ${ANIMATION})
    target_include_directories(ChronosEditor PUBLIC .)
endif()
