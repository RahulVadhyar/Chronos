file(GLOB RECTANGLE *.cpp)
target_sources(Chronos PRIVATE ${RECTANGLE})
target_include_directories(Chronos PUBLIC .)
if(CHRONOS_BUILD_EDITOR)
    target_sources(ChronosEditor PRIVATE ${RECTANGLE})
    target_include_directories(ChronosEditor PUBLIC .)
endif()